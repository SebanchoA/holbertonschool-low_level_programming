#include "main.h"

/**
 * main - check the code.
 *
 * Return: Always 0.
 */

int print_sign(int n)
{
  int a = "+";
  int b = "0";
  int c = "-";
  
  if (n > 0)
    {
      putchar (a);
      return (1);
    }
  else (n == 0)
    {
      
